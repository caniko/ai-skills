interface:
  display_name: "Grouped Git Commits"
  short_description: "Automatically commit every dirty worktree change"
  default_prompt: "Use $grouped-git-commits to inspect this repo and automatically commit every uncommitted change in coherent groups."
