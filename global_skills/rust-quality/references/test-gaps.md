# Test-Gaps Profile

Map public functions and important internal paths to meaningful tests. Cover
success, errors, boundaries, feature combinations, trait contracts, generic
implementations, and regression cases. Replace tests that only assert “does
not panic” with behavioral assertions where possible. Prioritize core behavior,
public API, and security-sensitive paths. Run focused tests first, then the
full project gate.
