# Graphify Policy

For cross-repository work, invoke `$graphify` before discovery, planning, or
edits. Query an existing graph; build or update a merged graph when it is
missing, stale, or incomplete. Reuse a current graph for the same repository
set.
