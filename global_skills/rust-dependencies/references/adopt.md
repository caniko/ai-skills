# Dependency Adoption Profile

Find hand-written parsers, retry loops, CLI handling, HTTP clients, date math,
or other behavior already provided by a maintained crate. Evaluate license,
maintenance, MSRV, feature cost, audit posture, and API fit. Skip trivial
implementations where a dependency adds more cost than it removes. Adopt one
candidate at a time and run compile/tests after each.
