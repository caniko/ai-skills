# Data Boundaries Profile

Find duplicated host, domain, service, user, port, package, secret, and
profile records, plus deep imports that bypass a stable interface. Choose one
pure source and one module/lib facade. Refactor consumers through evaluated
config or adapter helpers; document narrow allowlists for intentional direct
imports.

For integrated Home Manager prefer `osConfig`; keep standalone fallbacks
explicit. Verify generated aliases, routes, and services retain their values.
