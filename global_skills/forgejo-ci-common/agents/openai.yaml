interface:
  display_name: "Forgejo CI Common"
  short_description: "Shared Forgejo CI discovery and validation contract."
  default_prompt: "Use $forgejo-ci-common when a Forgejo CI skill directs you to its shared contract."
policy:
  allow_implicit_invocation: false
